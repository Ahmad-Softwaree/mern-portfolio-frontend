import { BACKEND_HOST } from "../../util/enum";

const SKILL_URL = `${BACKEND_HOST}/api/skill`;

export const GET_ALL_SKILL_URL = `${SKILL_URL}`;
export const GET_ONE_SKILL_URL = `${SKILL_URL}/one`;
export const ADD_SKILL_URL = `${SKILL_URL}`;
export const UPDATE_SKILL_URL = `${SKILL_URL}`;
export const DELETE_SKILL_URL = `${SKILL_URL}`;
