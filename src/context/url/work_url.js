import { BACKEND_HOST } from "../../util/enum";

const WORK_URL = `${BACKEND_HOST}/api/works`;

export const GET_ALL_WORK_URL = `${WORK_URL}`;
export const ADD_WORK_URL = `${WORK_URL}`;
export const UPDATE_WORK_URL = `${WORK_URL}`;
export const DELETE_WORK_URL = `${WORK_URL}`;
