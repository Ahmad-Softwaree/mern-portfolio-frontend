import { BACKEND_HOST } from "../../util/enum";

const CATEGORY_URL = `${BACKEND_HOST}/api/category`;

export const GET_ALL_CATEGORY_URL = `${CATEGORY_URL}`;
export const GET_ONE_CATEGORY_URL = `${CATEGORY_URL}`;
export const ADD_CATEGORY_URL = `${CATEGORY_URL}`;
export const UPDATE_CATEGORY_URL = `${CATEGORY_URL}`;
export const DELETE_CATEGORY_URL = `${CATEGORY_URL}`;
